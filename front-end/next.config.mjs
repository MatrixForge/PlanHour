/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        NEXT_PUBLIC_API_BASE_URL: 'http://localhost:5000/api', // Replace with your actual API base URL
    },
};

export default nextConfig;
